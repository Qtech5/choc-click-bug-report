// Copy this file to firebase-config.js and fill in the values from your
// Firebase project: Firebase console → Project settings → General →
// "Your apps" → SDK setup and configuration.
//
// These values are exposed to the client — that's expected for Firebase web
// apps. Data is protected by firestore.rules / storage.rules in this repo,
// not by hiding this config.

export const firebaseConfig = {
  apiKey: "",
  authDomain: "",
  projectId: "",
  storageBucket: "",
  messagingSenderId: "",
  appId: "",
};
